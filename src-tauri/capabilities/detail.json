{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "detail",
  "description": "Evidence window. Press-and-hold secret reveal allowed.",
  "windows": ["detail"],
  "permissions": [
    "core:default",
    "core:window:allow-set-title",
    "allow-reveal",
    "allow-scheduler",
    "allow-detail"
  ]
}
