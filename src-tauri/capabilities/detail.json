{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "detail",
  "description": "Evidence window. Press-and-hold secret reveal allowed.",
  "windows": ["detail"],
  "permissions": ["core:default", "allow-reveal", "allow-scheduler"]
}
